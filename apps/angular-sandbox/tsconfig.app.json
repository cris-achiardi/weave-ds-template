{
  "//": "THE BUILD config, and it exists because of one flag and one include. `tsconfig.json` beside it sets `noEmit: true`, which is correct for `pnpm typecheck` and fatal here: @analogjs/vite-plugin-angular uses this file to drive a real TypeScript emit, so `noEmit` makes it emit an EMPTY MODULE for every file — no error, a blank page. The `include` reaches outside this app on purpose: the plugin transforms only files inside its own program, so a .ts it cannot see arrives at Rollup as raw TypeScript and fails to parse on an import line, pointing at the wrong file entirely.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": false,
    "declaration": false,
    "sourceMap": true
  },
  "include": [
    "src/**/*.ts",
    "../../packages/angular/src/behavior/**/*.ts",
    "../../packages/behavior/src/**/*.ts"
  ],
  "exclude": ["../../packages/behavior/src/**/*.test.ts"]
}
