{
  "//": "The sandbox is typechecked because it is where GENERATED components land. `useDefineForClassFields` is false: a class field defined rather than assigned would shadow an accessor inherited from HTMLElement, which is exactly how a custom element's property/attribute reflection breaks.",
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "useDefineForClassFields": false,
    "types": ["vite/client"],
    "baseUrl": ".",
    "paths": {
      "@ds/wc/behavior": ["../../packages/wc/src/behavior/index.ts"],
      "@ds/wc": ["../../packages/wc/src/index.ts"]
    }
  },
  "include": ["src"]
}
