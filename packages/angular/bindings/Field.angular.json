{
  "$schema": "./binding.schema.json",
  "component": "Field",
  "contract": "../../contracts/components/Field/Field.contract.json",
  "framework": "angular",
  "notes": [
    "A pure composition wrapper: no state of its own, three referenced parts and the accessibility relationships between them."
  ]
}
