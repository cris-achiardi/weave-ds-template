{
  "$schema": "../../schema/component.schema.json",
  "component": "AccordionItem",
  "status": {
    "level": "experimental",
    "since": "2026-09-01",
    "note": "Only meaningful inside an Accordion."
  },
  "intent": {
    "purpose": "One section of an accordion: a heading that reveals a panel when chosen. It carries its own identity and its own disabled state, and nothing else — whether it is open is a comparison against the surrounding Accordion, not a property it holds.",
    "behaviour": [
      "Compares its `value` against the open set on the surrounding Accordion to decide whether it is open. It cannot be told directly.",
      "Toggles itself on click, and on Enter or Space when its trigger has focus.",
      "Renders its trigger inside a heading whose level the consumer supplies, because only the consumer knows the surrounding document outline.",
      "Points its trigger at its panel and names its panel from its trigger, so the two cannot disagree about which belongs to which.",
      "Marks its trigger disabled for assistive technology when it is the only open section in a single-open accordion, because activating it would do nothing.",
      "Throws if rendered outside an Accordion. There is no open set to compare against, and looking closed would hide the mistake."
    ],
    "notFor": [
      "Use outside an Accordion. It has no fallback behaviour and deliberately fails loudly instead.",
      "A standalone collapsible section. That is a disclosure component with no shared state and no coordination.",
      "Holding a heading level decision. The level is passed in; guessing it produces a document outline that is wrong in a way no gate here can detect."
    ]
  },
  "states": {
    "open": {
      "kind": "authored",
      "control": "internal",
      "description": "Derived from the surrounding Accordion by comparing `value`. Not settable on this component. Reaches assistive technology as an expanded state on the trigger.",
      "visual": "The panel is revealed and the trigger's indicator rotates."
    },
    "disabled": {
      "kind": "intrinsic",
      "control": "consumer",
      "description": "Set by the `disabled` prop or inherited from the Accordion. Rendered as a natively disabled button.",
      "visual": "The heading takes the disabled text colour and the pointer cursor is dropped."
    },
    "hover": {
      "kind": "intrinsic",
      "control": "internal",
      "description": "The pointer is over the trigger and it is not disabled.",
      "visual": "The heading row takes a wash."
    },
    "focus-visible": {
      "kind": "intrinsic",
      "control": "internal",
      "description": "The trigger is focused in a way the browser judges worth showing a ring for.",
      "visual": "A focus ring is drawn around the trigger."
    }
  },
  "semantics": {
    "role": "button",
    "focusable": true
  },
  "a11y": {
    "focusVisible": true,
    "contrast": "AA",
    "minHitArea": 44,
    "keyboard": "Tab reaches the trigger as an ordinary button. Enter and Space toggle the section.",
    "notes": [
      "The heading level is the consumer's to supply and there is no safe default. A component that guessed would produce a document outline that reads correctly to sighted users and wrongly to anyone navigating by heading — and nothing in this repo could detect it.",
      "The trigger must be the only element inside the heading. A decorative icon that is visually part of the row but semantically outside it has to sit outside the heading, or it is read as part of the section's name.",
      "The panel is named by its trigger. An item whose trigger holds a non-text child — an icon alone — gives its panel no usable name, and nothing here detects that.",
      "Animating the panel's height requires knowing that height, which is not available until the panel has been laid out. A consumer animating this must handle the first frame; the contract cannot promise a height it has not measured."
    ]
  },
  "composition": {
    "slots": {
      "heading": {
        "accepts": ["text"],
        "required": true,
        "max": 1,
        "description": "The section's name, rendered inside the trigger."
      },
      "panel": {
        "required": true,
        "max": 1,
        "description": "The revealed content. Anything."
      }
    }
  },
  "anatomy": {
    "root": {
      "part": "root",
      "description": "The section: a heading and the panel it reveals, kept adjacent so a single border can separate one section from the next.",
      "paints": {
        "border-block-end": null
      },
      "parts": {
        "header": {
          "part": "header",
          "description": "The heading element wrapping the trigger. Structural — it exists to carry the heading level and holds no paint of its own.",
          "internalOnly": true
        },
        "trigger": {
          "part": "trigger",
          "description": "The button. Owns the focus ring, the hit area and the hover wash, and stretches the full width so the whole row is the target.",
          "paints": {
            "padding": null,
            "color": null,
            "font-size": null,
            "font-weight": null,
            "min-block-size": null
          },
          "states": {
            "hover": {
              "background": null
            },
            "disabled": {
              "color": null
            },
            "focus-visible": {
              "outline": null,
              "outline-offset": null
            }
          },
          "parts": {
            "indicator": {
              "part": "indicator",
              "description": "The chevron marking whether the section is open. Rotates rather than swapping glyph, so the change reads as one object moving.",
              "paints": {
                "inline-size": null,
                "block-size": null,
                "color": null,
                "transform": null,
                "transition-duration": null
              },
              "states": {
                "open": {
                  "transform": null
                }
              }
            }
          }
        },
        "panel": {
          "part": "panel",
          "description": "The revealed content region.",
          "paints": {
            "padding": null,
            "color": null
          }
        }
      }
    }
  }
}
