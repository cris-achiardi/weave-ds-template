# React contract gate

`verify-contract.mjs` validates contracts and React bindings. `lib.mjs` supplies its
path and anatomy helpers. Run `pnpm verify:contract`.

Repository-wide contract views, the prop glossary, and consumer paint reports live in
[`scripts/`](../../../scripts/README.md). They read all registered backends without a build.
