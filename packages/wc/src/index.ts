// @ds/wc ships NO components, exactly as the other three backends ship none.
//
// Component source is generated from a contract into a consumer's own repository — see
// packages/wc/src/emit/README.md. The runtime this package does ship is the behaviour
// primitives, and they are a subpath: `@ds/wc/behavior`.
export {};
