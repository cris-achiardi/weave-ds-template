{
  "$schema": "./binding.schema.json",
  "component": "TextField",
  "contract": "../../contracts/components/TextField/TextField.contract.json",
  "framework": "wc",
  "notes": [
    "A native input inside the shadow root handles editing. The host participates in forms through ElementInternals because TextField.form declares a contribution. See packages/wc/forms.md."
  ]
}
