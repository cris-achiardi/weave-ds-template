{
  "_schema": "1.0",
  "_doc": "Project anchor for this design system's Figma integration. Single source of truth for which Figma file we read from, and for the identity that generation needs. Maps in maps/*.json resolve their `source` keys against this file. Never hard-code a file key in a script or a skill — read it from here, so pointing the system at a different file is one edit.",
  "lastUpdated": "2026-08-28",

  "bridges": {
    "_doc": "TWO bridges, recorded separately. The READ bridge is the only one anything depends on: the explore -> report -> decide -> build arc completes without the write bridge, and `pnpm verify` is green on a machine with neither Figma Desktop nor a plugin installed — which is what keeps the template's fresh-clone promise intact. The WRITE bridge is opt-in and unwired: no pnpm script, no gate and no CI job invokes it. Code stays canonical for token values in both directions; a difference is drift in Figma, never a change to adopt back.",

    "read": {
      "_doc": "The OFFICIAL Figma MCP connector. No daemon, no plugin, no Figma Desktop. Chosen because everything the arc needs is a READ, and every other bridge adds a local install and a background process that can fail.",
      "kind": "figma-mcp",
      "direction": "read",
      "requires": [],
      "tools": ["get_metadata", "get_variable_defs", "get_design_context", "get_screenshot"],
      "usedBy": [],
      "_usedByNote": "No skill. Reading the design source is a person's job, done when reference is needed — a scheduled sweep would imply an authority the canvas does not have. The call sequences live in README.md."
    },

    "write": {
      "_doc": "figma-console MCP driving the Desktop Bridge plugin. OPT-IN: nothing in this repo depends on it. Every write must first compare `figma.fileKey` against sources.<key>.key and pin the target with `figma_navigate({ url, lock: true })` — the active file drifts between open documents, and an unpinned write lands wherever the user last clicked. First exercised against this source on 2026-08-28.",
      "kind": "figma-console-mcp",
      "direction": "write",
      "requires": [
        "Figma Desktop running",
        "Desktop Bridge plugin open in the target file",
        "local figma-console MCP server (FIGMA_ACCESS_TOKEN only for the REST-backed tools)"
      ],
      "tools": [
        "figma_get_status",
        "figma_navigate",
        "figma_execute",
        "figma_capture_screenshot",
        "figma_get_variables"
      ],
      "usedBy": [
        ".claude/skills/ds-figma-component",
        ".claude/skills/ds-figma-document",
        ".claude/skills/ds-figma-explain"
      ],
      "gated": false,
      "_gatedNote": "NO gate exercises this bridge, so a generated Figma set can drift from its component with nothing failing anywhere. maps/components.json is a record, not a check. This is a known hole and it contradicts the repo's own enforcement rule — a contract whose breach produces no build error has to be gated in CI, or it is enforced on whichever machine happens to run it. Accepted at template stage because nothing has been generated yet; it should not survive contact with a real component library. The related quiet failure: every collection in this source has ONE mode, so a bound value and a baked literal render identically and a mis-bound component passes visual review. Read boundVariables back rather than trusting the render."
    }
  },

  "sources": {
    "weave": {
      "key": "eUO2kF0A3tJXdVTcvEPQjW",
      "name": "weave — Mokkap masterclass",
      "url": "https://www.figma.com/design/eUO2kF0A3tJXdVTcvEPQjW/weave---Mokkap-masterclass-29-08-2026",
      "purpose": "The design source. A dark screen-recorder UI. Variables, text styles and component sets live here.",
      "published": false,
      "libraryKey": null,
      "_publishedNote": "Not published as a library, so component instances have no durable componentKey yet. Until it is published the only join available is pageId + componentSetId, and those are file-local and change. Record them, but treat them as refreshable — see schema/components.schema.json.",
      "data": ["file", "variables", "styles", "components"]
    }
  },

  "identity": {
    "prefix": {
      "_doc": "Set once in /ds.config.json via `pnpm init-ds <name>`. Repeated here only so a Figma script has one file to read; ds.config.json is authoritative and init-ds rewrites both.",
      "name": "ds",
      "scope": "@ds",
      "token": "--ds",
      "data": "data-ds"
    },

    "variableNaming": {
      "_doc": "RE-MEASURED 2026-08-28 through the write bridge, correcting an earlier reading. A Figma variable carries TWO names and the previous entry conflated them: `name` is the slash path the API returns and the one you must look variables up by; `codeSyntax.WEB` is a separate author-set string that Figma emits in Dev Mode. The earlier `observed` list recorded codeSyntax values and labelled them names — which is where the phantom `weave-ds-` prefix came from. Both fields are real. All 87 variables carry both.",
      "figmaNamePattern": "{path}",
      "figmaCodeSyntaxPattern": "weave-ds-{path-flattened-to-dashes}",
      "cssPattern": "--{tokenPrefix}-{path}",
      "separator": "/",
      "separatorUnknown": false,
      "_separatorNote": "Confirmed by live read: group paths are slash-separated, which settles the DTCG nesting.",
      "observed": [
        "brand/primary",
        "brand/hover",
        "surface/primary",
        "surface/overlay",
        "surface/subtle",
        "surface/ghost",
        "border/primary",
        "text/primary",
        "text/secondary",
        "text/disabled",
        "text/inverted",
        "interactive/hover",
        "interactive/selectedBg",
        "control/waveform",
        "control/off",
        "color/purple/500",
        "color/gray/900",
        "space/0",
        "space/3",
        "radius/m",
        "radius/full"
      ],
      "observedCodeSyntax": [
        "brand/primary          -> codeSyntax.WEB: weave-ds-brand-primary",
        "surface/primary        -> codeSyntax.WEB: weave-ds-surface-primary",
        "color/purple/500       -> codeSyntax.WEB: weave-ds-color-purple-500",
        "interactive/selectedBg -> codeSyntax.WEB: weave-ds-interactive-selectedbg"
      ],
      "examples": [
        "space/3          -> --ds-space-3          -> space.3",
        "radius/m         -> --ds-radius-m         -> radius.m",
        "surface/primary  -> --ds-surface-primary  -> surface.primary"
      ],
      "knownProblems": [
        "PREFIX: DECIDED 2026-08-29 — `ds` stays. /ds.config.json remains `tokenPrefix: ds`, so tokens are `--ds-*` and packages are `@ds/*`; `pnpm init-ds` has deliberately NOT been run. Consequence: `codeSyntax.WEB` in Figma still emits `weave-ds-*`, which matches nothing in code. codeSyntax is an author-set display string that no build reads, so this is cosmetic in Dev Mode only — but it will mislead anyone copying from the Figma inspector. Fix it in Figma, never by hand-editing ds.config.json.",
        "SCALE VOCABULARY: DECIDED 2026-08-29 — numeric by amount, t-shirt by intent. `space/0..9`, `opacity/50..1000` and the colour steps are indexes; `radius/none..full` and `border/none..thick` are intents. One rule, two shapes. Recorded in packages/tokens/tokens/README.md §4 and ADR 0002; no Figma renames were needed.",
        "ICON SET NAMING: OUTSTANDING 2026-08-29 — ADR 0005 makes glyph names kebab-case in code (`graphic-eq`, `crop-16-9`), because .ai/maps/prop-map.md §2 requires kebab-case values. The 52 variants of the `icon` component set (2122:3051) are still snake_case, so the exact-match rule in variantConventions is BROKEN for every one of them until they are renamed in Figma. Two of the 52 are also slated for deletion under ADR 0004 — `graphic_eq_active` and `close_small_danger` are pure recolours. Nothing gates this; see maps/components.json -> icons.Icon.",
        "`interactive/selectedBg` is camelCase; every other token path is lowercase.",
        "`control/waveform` and `control/off` are component-specific but sit in the global namespace.",
        "Primitive variables have `scopes: []` while token variables carry `ALL_SCOPES`. Unscoped primitives are still offered everywhere in the picker, so nothing stops a designer binding straight to `color/purple/500`."
      ]
    },

    "font": {
      "_doc": "RE-MEASURED 2026-08-28: all 8 local text styles enumerated, not just the 5 seen on nodes. Every style is Lexend Deca and binds fontSize, fontFamily and fontWeight to variables — so type must be applied as a STYLE, never as a hand-set fontName + fontSize, or that linkage is thrown away.",
      "family": "Lexend Deca",
      "fallback": "sans-serif",
      "textStyles": [
        "UI/Button — Medium 16, lineHeight 124.875%",
        "UI/Label — Light 16, lineHeight AUTO",
        "UI/Label-emphasis — Medium 16, lineHeight AUTO",
        "UI/Description — Light 14, lineHeight AUTO",
        "UI/Caption — Light 12, lineHeight AUTO",
        "Display/Heading — Medium 20, lineHeight AUTO",
        "Display/Time — Medium 16, lineHeight AUTO",
        "Display/Title — Medium 24, lineHeight AUTO"
      ],
      "knownProblems": [
        "CORRECTS AN EARLIER READING. This previously said 'UI/Button uses 1.2487 while every other style uses 100'. The 100 was wrong: measured, the other seven styles are AUTO. AUTO is Figma's font-metric default, so seven of eight styles have NO controlled line height at all — the defect is larger than first recorded, not a disagreement between two values.",
        "`UI/Button`'s 124.87499713897705% is a dragged handle, not a designed value. Treat it as evidence when the type scale is decided, never as a number to reproduce.",
        "Decide whether the type scale owns line height before generating type tokens. Every component set generated before that is settled inherits the inconsistency."
      ]
    },

    "themes": {
      "_doc": "The source file has no light/dark axis — it is dark-only. Recorded here because dark-only BY DECISION and dark-only BY OMISSION are different states, and only one of them is safe to build on.",
      "modes": ["dark"],
      "decided": false
    },

    "variableCollections": {
      "_doc": "Figma collection -> canonical DTCG source file under packages/tokens/tokens/. Filled by hand from a live read on 2026-08-29; 154 variables transcribed and diffed name-by-name against the source, zero drift at that moment. One collection, one file — see ADR 0002. A Figma variable whose collection has no entry here resolves to `code: null` in maps/tokens.json and is reported as drift; that is the signal, not a silent gap.",
      "_driftWarning": "NOTHING GATES THIS. No script compares the JSON against Figma, so a variable edited, added or deleted in the design source produces no failure anywhere and the two surfaces simply disagree. The name-by-name diff that validated the first transcription was run by hand, once. Re-run it by hand whenever the design source moves. This is the known hole in ADR 0002 and it contradicts the repo's own rule that an ungated contract is enforced on whichever machine happens to run it.",
      "_transcribed": "2026-08-29",
      "map": {
        "Color Primitives": { "file": "color.palette.json", "tier": "primitive", "count": 71 },
        "Color Tokens": { "file": "color.semantic.json", "tier": "semantic", "count": 15 },
        "Type Primitives": { "file": "typography.palette.json", "tier": "primitive", "count": 10 },
        "Type Tokens": { "file": "typography.semantic.json", "tier": "semantic", "count": 24 },
        "Spacing Tokens": {
          "file": "spacing.json",
          "tier": "primitive",
          "count": 23,
          "note": "Carries three groups — space, radius, border — because Figma models them as one collection. The file split defers to the design source; see ADR 0002 trade-offs."
        },
        "Opacity Primitives": { "file": "opacity.json", "tier": "primitive", "count": 11 }
      }
    },

    "variantConventions": {
      "_doc": "A Figma variant property value must match the component's prop value exactly, or the two surfaces describe different components. The canon lives in .ai/maps/prop-map.md §1 and packages/react/prop-map.config.json — THOSE ARE AUTHORITATIVE; this is a pointer, not a copy.",
      "canonSource": [".ai/maps/prop-map.md", "packages/react/prop-map.config.json"],
      "propertyCase": "TitleCase property name, prop-exact value — Hierarchy=secondary, Size=m",
      "statesAreNotVariants": "Hover / Pressed / Focused are runtime states the browser owns. They must not become a variant property that maps to a prop. See .ai/maps/proposals/README.md."
    },

    "codePathConvention": {
      "pattern": "packages/react/src/components/<Name>/",
      "files": ["<Name>.tsx", "<Name>.module.css", "<Name>.contract.json", "index.ts"]
    }
  },

  "maps": {
    "tokens": "maps/tokens.json",
    "components": "maps/components.json"
  }
}
